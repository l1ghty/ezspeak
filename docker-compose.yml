services:
  ezspeak:
    build:
      context: .
      args:
        BUILD_DATE: ${BUILD_DATE:-}
    image: ezspeak:latest
    container_name: ezspeak
    ports:
      - "3111:3111"
    restart: unless-stopped
    environment:
      - PORT=3111
